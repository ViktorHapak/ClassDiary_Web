import { GradeListSlicerPipe } from './grade-list-slicer.pipe';

describe('GradeListSlicerPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeListSlicerPipe();
    expect(pipe).toBeTruthy();
  });
});
